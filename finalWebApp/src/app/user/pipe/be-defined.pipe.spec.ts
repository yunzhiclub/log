import { BeDefinedPipe } from './be-defined.pipe';

describe('BeDefinedPipe', () => {
  it('create an instance', () => {
    const pipe = new BeDefinedPipe();
    expect(pipe).toBeTruthy();
  });
});
